local _, ns = ... -- Namespace (myaddon, namespace)
local L = LibStub('AceLocale-3.0'):GetLocale('GroupGames') -- Locale

ns.tGame = {}
local tGame = ns.tGame

function tGame:Init()
end
function tGame:SetShown(val)
    
end
tGame:Init()